{{#each posts as |post| }}
<div class="post">
  <div>
    <h2>
      <a href="/post/{{post.id}}">{{post.title}}</a>
    </h2>
    <p>
        {{post.contents}}
    </p>
    <p>Created by {{post.user.name}}
      on {{format_date post.date_created}}
    </p>
</div>
{{/each}}
